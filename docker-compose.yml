services:
  api:
    build:
      context: api/
      dockerfile: Dockerfile
    ports:
      - "8000:80"
  frontend:
    build:
      context: frontend/
      dockerfile: Dockerfile
    ports:
      - "4543:8080"
    volumes:
      - ./frontend:/usr/src/app